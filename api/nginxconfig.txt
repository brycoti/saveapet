server {
       listen 80;
       listen [::]:80;

       server_name saveapet.westeurope.cloudapp.azure.com;

       root /var/www/saveapet;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}

server {
       listen 80;
       listen [::]:80;

       server_name m.saveapet.westeurope.cloudapp.azure.com;

       root /var/www/msaveapet;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}

server {
       listen 80;
       listen [::]:80;

       server_name api.saveapet.westeurope.cloudapp.azure.com;

        location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

}
